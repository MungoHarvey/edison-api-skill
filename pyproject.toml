[project]
name = "edison-api-skill"
version = "0.1.0"
requires-python = ">=3.11"

[dependency-groups]
dev = ["pytest>=8", "pytest-mock>=3.15.1"]

[tool.pytest.ini_options]
asyncio_mode = "strict"
addopts = "--cov=skills/_common --cov-report=term-missing --cov-fail-under=80"
